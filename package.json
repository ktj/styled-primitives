{
  "name": "styled-brimitives",
  "version": "0.0.9",
  "description": "styled components wrapper for react-brimitives",
  "main": "lib/",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prepublish": "rm -rf lib && babel src/ --out-dir lib/"
  },
  "dependencies": {
    "react-brimitives": ">=0.5.4",
    "styled-components": ">=2.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
